# ICS0017 — Currency Exchange Store

Run instructions
----------------

Build and run the project:

```bash
make
./build/exchange_store_cp1
```

Or run the provided tests:

```bash
bash tests/test_runner.sh
```